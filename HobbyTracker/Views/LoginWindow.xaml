<dx:ThemedWindow
    x:Class="HobbyTracker.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    Title="Hobby Tracker Giriş" 
    Icon="/HT.ico"
    Height="750" Width="1100" 
    WindowStartupLocation="CenterScreen"
    WindowStyle="None" 
    AllowsTransparency="True">

    <Grid Background="{StaticResource DarkBg}" MouseLeftButtonDown="Window_MouseDown">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="450"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Image Source="/ZImages/login-bg.png" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Border Opacity="0.80">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FF0C111A" Offset="0"/>
                        <GradientStop Color="Black" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <StackPanel VerticalAlignment="Center" Margin="60">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <Border Background="#1A2badee" CornerRadius="15" Padding="12,5" Margin="0,0,10,0">
                        <TextBlock Text="Oyun" Foreground="#2badee" FontWeight="Bold" FontSize="12"/>
                    </Border>
                    <Border Background="#1AA855F7" CornerRadius="15" Padding="12,5" Margin="0,0,10,0">
                        <TextBlock Text="Kitap" Foreground="#A855F7" FontWeight="Bold" FontSize="12"/>
                    </Border>
                    <Border Background="#1AFF0000" CornerRadius="15" Padding="12,5" Margin="0,0,10,0">
                        <TextBlock Text="Film" Foreground="#FFE81D16" FontWeight="Bold" FontSize="12"/>
                    </Border>
                    <Border Background="#1AF59E0B" CornerRadius="15" Padding="12,5">
                        <TextBlock Text="Dizi / Anime" Foreground="#F59E0B" FontWeight="Bold" FontSize="12"/>
                    </Border>
                </StackPanel>

                <TextBlock Text="Hobilerini Tek Yerden Yönet" FontSize="42" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Margin="0,0,0,20"/>
                <TextBlock Text="Oyun, Kitap, Film ve Dizi koleksiyonlarınızı düzenleyin, ilerlemenizi takip edin ve yeni dünyalar keşfedin." FontSize="16" Foreground="{StaticResource TextGray}" TextWrapping="Wrap" LineHeight="24"/>

                <StackPanel Orientation="Horizontal" Margin="0,40,0,0">
                    <StackPanel Margin="0,0,25,0">
                        <Image Width="32" Height="32">
                            <Image.Source>
                                <dx:SvgImageSource Uri="/ZImages/controller.svg"/>
                            </Image.Source>
                            <Image.Effect>
                                <DropShadowEffect Color="#2badee" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
                            </Image.Effect>
                        </Image>
                        <TextBlock Text="Oyunlar" Foreground="{StaticResource TextGray}" FontSize="12" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,25,0">
                        <Image Width="32" Height="32">
                            <Image.Source>
                                <dx:SvgImageSource Uri="/ZImages/book.svg"/>
                            </Image.Source>
                            <Image.Effect>
                                <DropShadowEffect Color="#A855F7" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
                            </Image.Effect>
                        </Image>
                        <TextBlock Text="Kitaplar" Foreground="{StaticResource TextGray}" FontSize="12" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,25,0">
                        <Image Width="32" Height="32">
                            <Image.Source>
                                <dx:SvgImageSource Uri="/ZImages/movie.svg"/>
                            </Image.Source>
                            <Image.Effect>
                                <DropShadowEffect Color="#F59E0B" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
                            </Image.Effect>
                        </Image>
                        <TextBlock Text="Filmler" Foreground="{StaticResource TextGray}" FontSize="12" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel>
                        <Image Width="32" Height="32">
                            <Image.Source>
                                <dx:SvgImageSource Uri="/ZImages/television.svg"/>
                            </Image.Source>
                            <Image.Effect>
                                <DropShadowEffect Color="#F59E0B" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
                            </Image.Effect>
                        </Image>
                        <TextBlock Text="Diziler" Foreground="{StaticResource TextGray}" FontSize="12" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1" Background="{StaticResource DarkBg}">
            <StackPanel VerticalAlignment="Center" Margin="50">
                <TextBlock Text="Tekrar Hoşgeldiniz" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                <TextBlock Text="Devam etmek için lütfen bilgilerinizi girin." Foreground="{StaticResource TextGray}" Margin="0,0,0,40"/>

                <TextBlock Foreground="{StaticResource TextGray}" Margin="0,0,0,8" FontSize="14" Text="E-posta Adresi"/>
                <Grid>
                    <dxe:TextEdit x:Name="txtUsername" Height="50" FontSize="14" Background="{StaticResource InputBg}" Foreground="White" BorderThickness="0" NullText="adiniz@ornek.com" CornerRadius="8" Padding="40,10,10,10"/>
                    <Image Width="20" HorizontalAlignment="Left" Margin="12,0,0,0" Opacity="0.7" IsHitTestVisible="False">
                        <Image.Source>
                            <dx:SvgImageSource Uri="/ZImages/mail.svg"/>
                        </Image.Source>
                    </Image>
                </Grid>

                <TextBlock Text="Şifre" Foreground="{StaticResource TextGray}" Margin="0,20,0,8" FontSize="14"/>
                <Grid>
                    <dxe:PasswordBoxEdit x:Name="txtPassword" Height="50" FontSize="14" Background="{StaticResource InputBg}" Foreground="White" BorderThickness="0" NullText="••••••••" CornerRadius="8" Padding="10,10,40,10" Visibility="Visible"/>
                    <dxe:TextEdit x:Name="txtVisiblePassword" Height="50" FontSize="14" Background="{StaticResource InputBg}" Foreground="White" BorderThickness="0" NullText="••••••••" CornerRadius="8" Padding="10,10,40,10" Visibility="Collapsed"/>

                    <Button Click="BtnTogglePassword_Click" HorizontalAlignment="Right" Margin="0,0,10,0" Background="Transparent" BorderThickness="0" Cursor="Hand" Width="30" Height="30">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Grid Background="Transparent">
                                    <ContentPresenter/>
                                </Grid>
                            </ControlTemplate>
                        </Button.Template>
                        <Grid>
                            <Image x:Name="imgEyeOpen" Width="20" Opacity="0.7" Visibility="Visible">
                                <Image.Source>
                                    <dx:SvgImageSource Uri="/ZImages/visibility.svg"/>
                                </Image.Source>
                            </Image>
                            <Image x:Name="imgEyeClosed" Width="20" Opacity="0.7" Visibility="Collapsed">
                                <Image.Source>
                                    <dx:SvgImageSource Uri="/ZImages/visibility_off.svg"/>
                                </Image.Source>
                            </Image>
                        </Grid>
                    </Button>
                </Grid>

                <Grid Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <dxe:CheckEdit x:Name="chkRememberMe" Content="Beni Hatırla" Foreground="#94a3b8" FontSize="12"/>
                    <TextBlock Grid.Column="1" Text="Şifremi unuttum?" Foreground="{StaticResource PrimaryBlue}" HorizontalAlignment="Right" VerticalAlignment="Center" Cursor="Hand" MouseLeftButtonDown="TxtForgotPassword_Click"/>
                </Grid>

                <dx:SimpleButton Content="Giriş Yap" Height="50" Margin="0,30,0,0" Background="{StaticResource PrimaryBlue}" Foreground="White" FontSize="16" FontWeight="Bold" CornerRadius="8" Cursor="Hand" IsDefault="True" Click="BtnLogin_Click"/>

                <Grid Margin="0,30,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Height="1" Background="#334155"/>
                    <TextBlock Grid.Column="1" Text="veya şununla devam et" Foreground="{StaticResource TextGray}" Margin="10,0" FontSize="12"/>
                    <Border Grid.Column="2" Height="1" Background="#334155"/>
                </Grid>

                <dx:SimpleButton Content="Google" Height="45" Background="{StaticResource InputBg}" Foreground="White" CornerRadius="8" Glyph="/ZImages/google.svg" GlyphHeight="24" GlyphWidth="24" GlyphToContentOffset="10"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                    <TextBlock Text="Hesabınız yok mu? " Foreground="{StaticResource TextGray}"/>
                    <TextBlock Text="Kayıt Ol" Foreground="{StaticResource PrimaryBlue}" FontWeight="Bold" Cursor="Hand" MouseLeftButtonDown="TxtRegister_Click"/>
                </StackPanel>
            </StackPanel>

            <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20" Foreground="White" Opacity="0.9" Background="Transparent" BorderThickness="0" FontSize="24" FontWeight="Bold" Cursor="Hand" Click="BtnClose_Click"/>
        </Grid>
    </Grid>
</dx:ThemedWindow>